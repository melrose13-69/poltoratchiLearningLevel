
const initialState = {
    friends: [
        {name: 'ololo', id: 10},
        {name: 'asdaa', id: 11},
        {name: 'ad3,l', id: 12}
    ]
};

const sidebarReducer = (state = initialState) => {
    return state
};

export default sidebarReducer;